package assets;
import javax.swing.*;

public class Mainmenu extends JButton {

    /*int mineCount;
    int numRows;
    int tileSize;
    
    Minesweeper game = new Minesweeper(tileSize, numRows, mineCount);
    */
}
